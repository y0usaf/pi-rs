---
description: Launch or resume dependency-safe pi-rs parallel worktree orchestration
argument-hint: "[run|status|plan] [scope constraints]"
---
Read `.pi/skills/orchestrate/SKILL.md` in full and execute it as the sole orchestrator.

Mode: `${1:-run}`
Additional constraints: `${@:2}`
