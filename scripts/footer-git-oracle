#!/usr/bin/env bash
# Regenerate the footer-git-parity oracle from Pi's real
# footer-data-provider.ts. See the sibling frontmatter-oracle.
set -euo pipefail
cd "$(dirname "$0")/.."
if [[ ! -x ref/pi/node_modules/.bin/tsx ]]; then
  echo "ref/pi/node_modules missing tsx; run npm ci in ref/pi" >&2
  exit 1
fi
nix shell nixpkgs#nodejs_22 --command \
  ref/pi/node_modules/.bin/tsx --tsconfig ref/pi/tsconfig.json \
  tests/footer-git-parity/gen-oracle.ts \
  tests/footer-git-parity/oracle.json
echo "wrote tests/footer-git-parity/oracle.json"