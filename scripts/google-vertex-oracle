#!/usr/bin/env bash
set -euo pipefail
cd "$(dirname "$0")/.."
if [[ ! -x ref/pi/node_modules/.bin/tsx ]]; then
  nix shell nixpkgs#nodejs_22 --command sh -c 'cd ref/pi && corepack npm ci --ignore-scripts'
fi
env -u GEMINI_API_KEY -u GOOGLE_API_KEY nix shell nixpkgs#nodejs_22 --command ref/pi/node_modules/.bin/tsx --tsconfig ref/pi/tsconfig.json tests/google-vertex-parity/gen-oracle.ts tests/google-vertex-parity/cases.json > tests/google-vertex-parity/oracle.json
echo "wrote tests/google-vertex-parity/oracle.json"
