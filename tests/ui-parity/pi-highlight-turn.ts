// Syntax-highlighting transcript scenario (tagged fenced code blocks in an
// assistant message). Same driver as the basic turn; the scenario JSON
// carries the highlighted content.
import "./pi-basic-turn.ts";
