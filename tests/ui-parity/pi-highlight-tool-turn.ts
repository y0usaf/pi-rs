// Syntax-highlighting tool scenario (read .ts / write .py render their
// file content through highlightCode). Same driver as the tool turn; the
// scenario JSON carries the highlighted content.
import "./pi-tool-turn.ts";
